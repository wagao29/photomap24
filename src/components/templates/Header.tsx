import iconLogo from '../../assets/title_logo.svg';

const Header = () => {
  return <img src={iconLogo} width={180} height={180} className='absolute z-10 top-1 left-1' />;
};

export default Header;
